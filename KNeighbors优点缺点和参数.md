# KNeighbors 优点、缺点和参数

一般来说，KNeighbors分类器有2个重要参数：

1.邻居个数

2.数据点之间距离的度量方法

在实践中使用较小的邻居个数（比如3个或5个）往往可以得到比较好的结果，在实际操作中该参数可以调节。默认使用欧式距离。

## 1.优点

K-NN的优点之一就是模型容易理解，通常不需要过多调节就可以得到不错的性能。在考虑使用更高级的技术之前，尝试K-NN算法是一种很好的基准方法。构建最近邻模型的速度通常很快，但如果训练集很大（特征数很多或样本数很大），预测速度可能会比较慢。使用K-NN算法时，对数据进行预处理非常重要！！！

## 2.缺点

K-NN算法对于有很多特征（几百或更多）的数据集往往不好，对于大多数特征的大多取值为0的数据集（所谓稀疏集）来说，K-NN算法的效果尤其不好。

# 总结

虽然K近邻算法很容易理解，但由于其预测速度慢且不能处理具有很多特征的数据集，所以在实践中往往不会用到。
